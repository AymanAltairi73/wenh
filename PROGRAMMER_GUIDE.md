# وصف فكرة تطبيق «وينه؟» للمبرمج

## 1️⃣ اسم التطبيق:
**وينه؟**

## 2️⃣ الهدف:
تطبيق مخصص للعمال المشتركين، يجمع طلبات المستخدمين النهائيين بدون تسجيل أو دفع، ويعرضها فقط للعمال الذين دفعوا اشتراكًا أسبوعي أو شهري.

---

## 3️⃣ المستخدمون

### أ) الزبون النهائي:
- لا تسجيل حساب
- لا دفع أونلاين
- فقط يرسل طلب عامل لأي عمل: كهرباء، سباكة، تنظيف، نقل، دهان، تصليح، أو أي عمل آخر
- يحدد المنطقة
- يكتب وصف مختصر (اختياري)
- يضغط "إرسال الطلب" → الطلب يُخزن في قاعدة البيانات

### ب) العامل المشترك:
- تسجيل دخول بحساب واحد
- يختار خطة الاشتراك:
  - أسبوعي (مثلاً 5–10 ألف دينار)
  - شهري (15–25 ألف دينار)
- يراقب الطلبات حسب المنطقة أو نوع العمل
- يضغط على أي طلب لاستلامه → يؤكد استلامه → يظهر حالة الطلب "مأخوذ"

### ج) الأدمن (أنت):
- رؤية كل الطلبات الواردة
- إدارة العمال المشتركين + اشتراكاتهم
- تحديث حالة الطلبات
- متابعة الإيرادات

---

## 4️⃣ قاعدة البيانات (Firebase Realtime Database أو Firestore)

### Requests Table (الطلبات)
```json
{
  "request_id": {
    "type": "كهرباء",
    "area": "المنطقة",
    "description": "تفاصيل العمل",
    "status": "جديد",
    "timestamp": "تاريخ الإرسال",
    "createdBy": "anonymous",
    "takenBy": null
  }
}
```

### Workers Table (العمال المشتركين)
```json
{
  "worker_id": {
    "name": "اسم العامل",
    "email": "email@example.com",
    "phone": "رقم الهاتف",
    "subscription": true,
    "subscriptionActive": true,
    "subscriptionPlan": "أسبوعي",
    "subscriptionStart": "تاريخ البدء",
    "subscriptionEnd": "تاريخ الانتهاء"
  }
}
```

---

## 5️⃣ واجهات التطبيق

### أ) MainActivity (الزبون)
- شاشة البداية: شعار + زر "طلب عامل"
- نموذج الطلب: نوع العمل، المنطقة، وصف العمل (اختياري)
- زر "إرسال الطلب"
- رسالة تأكيد: "تم استلام طلبك. سيصلك العامل المناسب قريبًا."

### ب) WorkerActivity (العامل المشترك)
- تسجيل دخول
- عرض الطلبات المتاحة حسب المنطقة/التخصص
- اختيار الطلبات واستلامها
- إظهار حالة الاشتراك (نشط / انتهى)

### ج) AdminActivity (الأدمن)
- عرض الطلبات جميعها
- عرض جميع المشتركين + اشتراكاتهم
- تعديل حالة الطلبات
- مراقبة الإيرادات والاشتراكات

---

## 6️⃣ ملاحظات التصميم
- ألوان: خلفية أبيض، أزرار أخضر، نص أسود
- خط عربي واضح (Cairo أو Noto Sans Arabic)
- شعار التطبيق: «وينه؟ – اطلب عامل لأي شغل»
- تجربة مستخدم بسيطة جدًا بدون تعقيد

---

## 7️⃣ النسخة التجريبية
يمكن البدء بـ Web App / Google Forms لتجربة السوق قبل برمجة التطبيق الكامل
- الزبون يرسل الطلبات → تظهر للعامل المشترك → يؤكد استلام الطلب
- بعد اختبار الفكرة، الانتقال لتطبيق Android رسمي

---

## 8️⃣ الهيكل الحالي للمشروع (Flutter)

### الملفات الرئيسية:
- `lib/main.dart` - نقطة البداية
- `lib/app.dart` - إعدادات التطبيق والـ Providers
- `lib/models/` - نماذج البيانات
- `lib/screens/` - واجهات المستخدم
- `lib/services/` - الخدمات و Firebase
- `lib/cubits/` - إدارة الحالة (BLoC pattern)
- `lib/widgets/` - مكونات واجهة المستخدم
- `lib/utils/` - أدوات مساعدة

### النماذج الحالية:
- `RequestModel` - يمثل طلب العميل
- `WorkerModel` - يمثل بيانات العامل المشترك

### الواجهات الحالية:
- `SplashScreen` - شاشة البداية
- `RoleSelection` - اختيار نوع المستخدم
- `FixedCustomerHomeScreen` - واجهة العميل
- `SendRequestScreen` - إرسال طلب جديد
- `WorkerLogin` - تسجيل دخول العامل
- `EnhancedWorkerRequestsScreen` - عرض الطلبات للعامل
- `AdminLoginScreen` - تسجيل دخول الأدمن
- `EnhancedAdminDashboard` - لوحة تحكم الأدمن

---

## 9️⃣ التقنيات المستخدمة
- **Flutter** - إطار العمل الرئيسي
- **Firebase** - قاعدة البيانات والمصادقة
- **BLoC Pattern** - إدارة الحالة
- **Google Fonts** - الخطوط العربية
- **Cupertino Icons** - الأيقونات

---

## 🔟 متطلبات التطوير

### 1. تحسين نموذج الطلب:
```dart
class RequestModel {
  final String id;
  final String type; // كهرباء، سباكة، تنظيف، إلخ
  final String area; // المنطقة
  final String description; // وصف العمل
  final String status; // 'new', 'taken', 'completed'
  final String? takenBy; // معرف العامل الذي استلم الطلب
  final DateTime timestamp; // وقت الإرسال
}
```

### 2. تحسين نموذج العامل:
```dart
class WorkerModel {
  final String uid;
  final String name;
  final String email;
  final String phone;
  final String subscriptionPlan; // 'weekly', 'monthly', 'none'
  final DateTime subscriptionStart;
  final DateTime subscriptionEnd;
  final bool subscriptionActive;
}
```

### 3. الوظائف المطلوبة:
- إرسال طلب جديد (بدون تسجيل)
- عرض الطلبات المتاحة للعمال المشتركين فقط
- استلام الطلب من قبل العامل
- التحقق من صلاحية الاشتراك
- لوحة تحكم الأدمن الكاملة

---

## 1️⃣1️⃣ خطوات التنفيذ المقترحة

1. **تأمين الوصول للعمال فقط** - التحقق من الاشتراك قبل عرض الطلبات
2. **تحسين واجهة العميل** - جعلها أبسط وأكثر وضوحًا
3. **完善 نظام الاشتراكات** - إدارة الاشتراكات الأسبوعية والشهرية
4. **تحسين لوحة الأدمن** - إحصائيات وإدارة شاملة
5. **اختبار شامل** - تأكد من عمل جميع الوظائف

---

## 1️⃣2️⃣ ملاحظات هامة
- يجب أن يكون التطبيق باللغة العربية بالكامل
- التركيز على البساطة وسهولة الاستخدام
- التأكد من أن العملاء لا يحتاجون للتسجيل
- العمال فقط هم من يحتاجون للتسجيل والدفع
- الأدمن لديه صلاحيات كاملة على النظام

---

هسه تقدر تنسخ هذا النص بالكامل وتدزّه لأي مبرمج، وهو يفهم كل التفاصيل ويقدر يبني لك التطبيق بالضبط حسب متطلباتك.

إذا تحب، أگدر أسوي لك مخطط رسومي للواجهات جاهز للمبرمج حتى يسهل عليه الشغل أكثر.
